# Dependencies for Chronicle setup scripts (root wizard + all service init scripts)
#
# The relative path below is resolved by uv against the *working directory*, not
# against this file, so setup commands must be run from the repository root:
#   uv run --with-requirements setup-requirements.txt python extras/<svc>/init.py
#
# Keep the `-e`: without it, edits to chronicle_setup/ are silently ignored.
# See docs/init-system.md#why-chronicle-setup-is-installed-editable
-e ./extras/chronicle-setup
rich>=13.0.0
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0.0
ruamel-yaml>=0.18.0
omegaconf>=2.3.0
minidisc-python>=0.1.0

# Deployment policy validation and streaming service gateway.
pydantic>=2.0,<3
httpx>=0.28,<1
